{
    "name": "password-generate",
    "version": "0.0.0",
    "scripts": {
        "predeploy": "yarn build",
        "deploy": "gh-page -d dist",
        "dev": "vite",
        "build": "vite build",
        "serve": "vite preview"
    },
    "homepage": "http://brahyanpro.github.io/password-generate",
    "dependencies": {
        "preact": "^10.5.13",
        "terminal.css": "^0.7.2"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.0.0",
        "gh-pages": "^3.2.3",
        "vite": "^2.6.4"
    }
}